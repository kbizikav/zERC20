# zkp

## Coverage
We use `cargo-llvm-cov` for coverage reports.

Install the tool:

```bash
cargo install cargo-llvm-cov
```

Run coverage for this crate:

```bash
./zkp/coverage.sh
```

HTML report output:

- `zkp/coverage/html/index.html`
