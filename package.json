{
  "name": "zerc20-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "pnpm --filter frontend build",
    "dev": "pnpm --filter frontend dev",
    "test": "pnpm --filter frontend test",
    "typecheck": "pnpm --filter frontend typecheck"
  }
}
